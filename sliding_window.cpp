#include "sliding_window.h"

int main(int argc, char* argv[])
{

    if (argc == 2)
    {
        // send
    }

    else if (argc == 1)
    {
        // receive
    }

    else
    {
        fprintf(stderr, "usage: sliding_window hostname");
        exit(1);
    }

    return 0;
}
